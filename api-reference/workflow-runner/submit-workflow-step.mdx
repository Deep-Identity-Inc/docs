---
title: Submit Workflow Step
description: Submit and execute one step of a workflow session
api: POST /v1/sessions/{session_id}/steps/{step_id}
---

```
POST /v1/sessions/{session_id}/steps/{step_id}
```

Submits an action or data for one workflow step. The request body's shape is **config-driven** — it depends on `step_id`, its current phase, and the settings configured on the workflow. Asynchronous and multi-phase steps can return an in-progress state instead of completing synchronously.

Steps must be submitted **in order**: the `step_id` in the URL must match the session's `current_step` (as returned by [Create Workflow Session](/api-reference/workflow-runner/create-workflow-session) or [Get Workflow State](/api-reference/workflow-runner/get-workflow-state)), and that step must be `PENDING` or `IN_PROGRESS`.

## Request

### Headers

| Header | Required | Description |
|---|---|---|
| `x-api-key` | Yes | Your organization's API key |

### Path parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `session_id` | string (uuid) | Yes | The session to submit a step for |
| `step_id` | string | Yes | A supported workflow step ID — must match the session's current step |

### Body parameters

The generated OpenAPI request schema lists every supported step submission. Use the current step's `requirements` from [Create Workflow Session](/api-reference/workflow-runner/create-workflow-session) or [Get Workflow State](/api-reference/workflow-runner/get-workflow-state) to select the correct body.

### Request pattern by step

| Step ID | Submission body |
|---|---|
| `ID_VERIFICATION` | Document types and an `uploads` map containing the required ID/selfie keys |
| `FACE_LIVENESS` | `{ "action": "start" }`, `{ "action": "complete" }`, or the custom-provider `upload-url` action |
| `FACE_LIVENESS_CONSENT_SETTINGS` | `{}` when consent is disabled; otherwise the same body as `CONSENT` |
| `AGE_ESTIMATION` | `{ "uploads": { "selfie_front": "..." } }` |
| `DEEPFAKE_DETECTION` | `start` to receive capture instructions, then `complete` with the returned frame/timeline upload keys |
| `DEEP_AGE`, `ADDRESS_VERIFICATION`, `BACKGROUND_CHECK` | `{ "action": "start" }`, followed by `{ "action": "complete" }` after the hosted phase finishes |
| `CREDIT_CHECK` | Canadian address, date of birth, SIN, and explicit credit/privacy consent |
| `KYB` | Business, registered-address, ownership, officer, and configured document data |
| `TITLE_SEARCH` | `{ "address": "..." }` |
| `PEP_SANCTIONS` | `first_name`, `last_name`, `date_of_birth`, and optional `email` |
| `ADVERSE_MEDIA` | `start` with the screening identity, then `complete` while polling |
| `CUSTOM_PROMPT` | `uploads` keyed by the slots returned in `requirements` |
| `CONSENT` | `accepted`; when accepted, `legal_name` and any required `signature_upload` |
| `BANK_STATEMENT_UPLOAD` | `start` to receive the hosted request, then `complete` after upload finishes |
| `AI_BANK_STATEMENT_ANALYSIS` | `{}` |
| `DOCUMENT_UPLOAD` | `uploads` keyed by the configured document slots returned in `requirements` |
| `CUSTOM_FORM` | `answers` keyed by the fields returned in `requirements` |
| `WHITE_LABEL` | `{ "acknowledged": true }` after applying the returned presentation settings |
| `PHONE_TRUST_CHECK`, `CARRIER_AGE_GATE`, `PHONE_OWNERSHIP_MATCH` | `{}`; these executors use applicant data already stored on the session |
| `PHONE_VERIFICATION` | `{ "action": "start" }`, followed by `complete` polling |
| `PROOFCALL` | `start` with 1–3 references, followed by `complete` polling |

The detailed examples below cover upload, presentation, server-auto, and multi-phase execution. For larger schemas such as `CREDIT_CHECK`, `KYB`, `CUSTOM_FORM`, and `DEEPFAKE_DETECTION`, use the generated request schema on this page together with the current step's `requirements`.

## Step submission schemas

### ID_VERIFICATION

<Info>
  Get `uploads` values from [Create Session Uploads](/api-reference/workflow-runner/create-session-uploads) — upload each image to its presigned URL first, then submit the returned `file_key` values here.
</Info>

| Field | Type | Required | Description |
|---|---|---|---|
| `document_type` | string | Yes | The type of the primary document being submitted, e.g. `passport-us` |
| `secondary_document_type` | string | Only if the step requires a secondary ID | Type of the secondary document |
| `tertiary_document_type` | string | Only if the step requires a tertiary ID | Type of the tertiary document |
| `uploads.id_front` | string | Yes | File key for the primary document's front image |
| `uploads.id_back` | string | Yes, unless `document_type` is a front-only type (see `front_only_document_types` in [requirements](/api-reference/workflow-runner/create-workflow-session#id_verification-requirements)) | File key for the primary document's back image. Rejected if supplied for a front-only document type |
| `uploads.secondary_id_front` | string | Only if a secondary ID is required | File key for the secondary document's front image |
| `uploads.secondary_id_back` | string | Same front/back rule as `id_back`, keyed off `secondary_document_type` | File key for the secondary document's back image |
| `uploads.tertiary_id_front` | string | Only if a tertiary ID is required | File key for the tertiary document's front image |
| `uploads.tertiary_id_back` | string | Same front/back rule as `id_back`, keyed off `tertiary_document_type` | File key for the tertiary document's back image |
| `uploads.selfie_front` | string | Yes | File key for the selfie image |

Which of these fields are required or accepted for a given session depends on that step's configuration (whether it requires a secondary/tertiary ID), not on this table alone — check the step's `requirements` from [Create Workflow Session](/api-reference/workflow-runner/create-workflow-session#id_verification-requirements) or [Get Workflow State](/api-reference/workflow-runner/get-workflow-state).

<CodeGroup>

```bash cURL
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/ID_VERIFICATION \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "document_type": "passport-us",
    "uploads": {
      "id_front": "3f9c1e2a-org/80e801af.../1717000000000-id_front.jpg",
      "selfie_front": "3f9c1e2a-org/80e801af.../1717000000001-selfie_front.jpg"
    }
  }'
```

```javascript Node.js
const response = await fetch(
  "https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/ID_VERIFICATION",
  {
    method: "POST",
    headers: {
      "x-api-key": "YOUR_API_KEY",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      document_type: "passport-us",
      uploads: {
        id_front: "3f9c1e2a-org/80e801af.../1717000000000-id_front.jpg",
        selfie_front: "3f9c1e2a-org/80e801af.../1717000000001-selfie_front.jpg",
      },
    }),
  }
);

const result = await response.json();
```

```python Python
import requests

response = requests.post(
    "https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/ID_VERIFICATION",
    headers={"x-api-key": "YOUR_API_KEY"},
    json={
        "document_type": "passport-us",
        "uploads": {
            "id_front": "3f9c1e2a-org/80e801af.../1717000000000-id_front.jpg",
            "selfie_front": "3f9c1e2a-org/80e801af.../1717000000001-selfie_front.jpg",
        },
    },
)

result = response.json()
```

</CodeGroup>

### FACE_LIVENESS

The body is a discriminated union on `action`. All variants also accept `frame_count` and `clip_mime_type`, but those are only meaningful — and only validated — when `action` is `upload-url`.

| `action` | Fields | Description |
|---|---|---|
| `start` | none | Mints a Rekognition Face Liveness session and returns streaming credentials for the client SDK |
| `complete` | none | Fetches the liveness result server-side once the client has finished streaming |
| `upload-url` | `frame_count` (integer, 1-8, required), `clip_mime_type` (string, optional) | Custom liveness provider only — mints upload URLs for captured frames/clip. Not used with the default provider |

<CodeGroup>

```bash cURL — start
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/FACE_LIVENESS \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "action": "start" }'
```

```bash cURL — complete
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/FACE_LIVENESS \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "action": "complete" }'
```

```javascript Node.js
const start = await fetch(
  "https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/FACE_LIVENESS",
  {
    method: "POST",
    headers: { "x-api-key": "YOUR_API_KEY", "Content-Type": "application/json" },
    body: JSON.stringify({ action: "start" }),
  }
);

// ... stream the liveness session with the returned credentials, then:

const complete = await fetch(
  "https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/FACE_LIVENESS",
  {
    method: "POST",
    headers: { "x-api-key": "YOUR_API_KEY", "Content-Type": "application/json" },
    body: JSON.stringify({ action: "complete" }),
  }
);
```

```python Python
import requests

headers = {"x-api-key": "YOUR_API_KEY"}
base = "https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/FACE_LIVENESS"

start = requests.post(base, headers=headers, json={"action": "start"}).json()

# ... stream the liveness session with the returned credentials, then:

complete = requests.post(base, headers=headers, json={"action": "complete"}).json()
```

</CodeGroup>

### Server-auto phone checks

`PHONE_TRUST_CHECK`, `CARRIER_AGE_GATE`, and `PHONE_OWNERSHIP_MATCH` read the applicant data supplied when the headless session was created. Submit an empty JSON object when the step becomes current.

```bash
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/PHONE_TRUST_CHECK \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```

```json 200 — Phone Trust completed
{
  "step_id": "PHONE_TRUST_CHECK",
  "step_status": "COMPLETED",
  "failure_reason": null,
  "current_step": 1,
  "attempts_remaining": 3,
  "session_status": "PENDING",
  "session_progress": "STARTED",
  "phone_trust_check": {
    "tripped": false,
    "tripReasons": [],
    "simSwappedRisk": "LOW_RISK",
    "forwardingRisk": "NO_RISK",
    "sensitivity": "medium",
    "statusMessage": "Response from one supplier",
    "checkedAt": "2026-09-08T14:04:00.000Z"
  }
}
```

### CONSENT

When `accepted` is `true`, `legal_name` is required. If the workflow configuration requires a signature, upload it first and include its session-owned key as `signature_upload`. A declined consent completes the attempt with `failure_reason: "CONSENT_DECLINED"`.

```bash
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/CONSENT \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "accepted": true,
    "legal_name": "Jane Doe"
  }'
```

### PHONE_VERIFICATION

Call `start` once. It returns `IN_PROGRESS` while the phone call is being placed. Wait for the `poll_interval_seconds` returned in the step requirements, then call `complete` until the result is ready.

```bash cURL — start
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/PHONE_VERIFICATION \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "action": "start" }'
```

```json 200 — Call started
{
  "step_id": "PHONE_VERIFICATION",
  "step_status": "IN_PROGRESS",
  "failure_reason": null,
  "current_step": 0,
  "attempts_remaining": 3,
  "session_status": "PENDING",
  "session_progress": "STARTED",
  "phone_verification": {
    "status": "pending",
    "reused": false
  }
}
```

```bash cURL — poll result
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/PHONE_VERIFICATION \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "action": "complete" }'
```

A pending result returns `409` with `failure_reason: "PHONE_VERIFICATION_RESULT_NOT_READY"`. A completed mismatch returns `200` with `PHONE_VERIFICATION_CHECK_FAILED` and consumes an attempt.

### PROOFCALL

Supply between one and three references with `start`. The `complete` action takes no references and should be polled at the interval returned in `requirements`.

```bash cURL — start
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/PROOFCALL \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "start",
    "references": [
      {
        "first_name": "Ada",
        "last_name": "Lovelace",
        "phone": "+14155551234",
        "email": "ada@example.com",
        "relationship": "Former manager"
      }
    ]
  }'
```

```json 200 — ProofCall started
{
  "step_id": "PROOFCALL",
  "step_status": "IN_PROGRESS",
  "failure_reason": null,
  "current_step": 0,
  "attempts_remaining": 3,
  "session_status": "PENDING",
  "session_progress": "STARTED",
  "proof_call": {
    "status": "pending",
    "operation_id": "8d4cd77e-79f7-5ad7-a916-8852f5f9c577"
  }
}
```

```bash cURL — poll result
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/steps/PROOFCALL \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "action": "complete" }'
```

A result that is not ready returns `409` with `failure_reason: "PROOF_CALL_RESULT_NOT_READY"`. A ready response includes `proof_call.verdict` (`pass`, `threshold`, or `fail`) and per-reference answered-question counts.

## Response

The response uses a common envelope for every supported step. Individual executors can add step-specific result fields; for example, `FACE_LIVENESS` adds a `liveness` object.

### 200 — Success

| Field | Type | Present when | Description |
|---|---|---|---|
| `step_id` | string | always | The submitted workflow step ID |
| `step_status` | string | always | `PENDING`, `IN_PROGRESS`, `COMPLETED`, `FAILED`, or `SKIPPED` |
| `failure_reason` | string \| null | always | Step-specific reason the attempt failed, or `null`. The sections below list the ID verification and face-liveness values |
| `current_step` | integer \| null | always | 0-based index of the next step to submit. `null` once the run has finished |
| `attempts_remaining` | number \| null | always | Session-wide failure budget remaining. `null` when the workflow has no failure limit configured |
| `session_status` | string | always | Session status: `PENDING`, `SUBMITTED`, `VERIFIED`, `REJECTED`, `VOIDED`, `EXPIRED`, or `FAILED` |
| `session_progress` | string | always | `PENDING`, `STARTED`, or `COMPLETED` |
| `liveness` | object | `step_id` is `FACE_LIVENESS` | Shape depends on the submitted `action` — see [FACE_LIVENESS results](#face_liveness-results) |
| Step-specific result | object | executor-dependent | A snake_case key such as `phone_trust_check`, `credit_check`, `phone_verification`, or `proof_call`. Treat new fields as additive |

<Note>
  Advisory checks (age, expiry, document validity period, state) never fail an `ID_VERIFICATION` step and are not included in the response — only the blocking checks below can produce a `failure_reason`.
</Note>

### ID_VERIFICATION failure reasons

| `failure_reason` | Meaning |
|---|---|
| `FACE_NOT_DETECTED_IN_ID` | No face was detected on the primary document's front image |
| `OCR_NAME_MISSING` | No first/last/middle name could be extracted from the document |
| `ID_CLASSIFICATION_FAILED` | The document's detected type doesn't match, or isn't an accepted, document type |
| `ID_TYPE_UNRECOGNIZED` | The document's type field couldn't be read |
| `ID_TYPE_LOW_CONFIDENCE` | The document's type field was read with too little confidence |
| `DOC_TYPE_NOT_ALLOWED` | The detected document type doesn't match the declared `document_type` family |
| `NO_FACE_DETECTED` | No face was detected in the selfie image |
| `FACE_MISMATCH` | The selfie face doesn't match the document's face closely enough |
| `LIVENESS_ID_MISMATCH` | The face captured during the `FACE_LIVENESS` step doesn't match the document's face |

### FACE_LIVENESS results

`liveness` depends on the submitted `action`:

**`start`**

| Field | Type | Description |
|---|---|---|
| `liveness_session_id` | string | The Rekognition Face Liveness session ID the client streams to |
| `region` | string | AWS region for the liveness session, e.g. `us-east-1` |
| `credentials.access_key_id` | string | Temporary AWS access key |
| `credentials.secret_access_key` | string | Temporary AWS secret key |
| `credentials.session_token` | string | Temporary AWS session token |
| `credentials.expiration` | string | ISO 8601 expiry of the credentials (~15 minutes) |
| `expires_at` | string | ISO 8601 expiry of the liveness session |

**`complete`**

| Field | Type | Description |
|---|---|---|
| `status` | string | Normalized Rekognition liveness status, e.g. `SUCCEEDED` or `FAILED` |
| `confidence` | number \| null | Liveness confidence score (0-100), or `null` when Rekognition didn't return one |
| `passed` | boolean | `true` when `status` is `SUCCEEDED` and `confidence` meets the step's configured threshold |

### FACE_LIVENESS failure reasons

| `failure_reason` | Meaning |
|---|---|
| `FACE_LIVENESS_CHECK_FAILED` | The liveness check completed but did not pass (`passed: false`). Returned in a `200` response, not an error |

<Note>
  Calling `complete` before `start`, or before Rekognition has finished processing, returns a `409` with `failure_reason` of `FACE_LIVENESS_NOT_STARTED` or `FACE_LIVENESS_RESULT_NOT_READY` — see [Error responses](#error-responses). These are retryable and don't consume an attempt.
</Note>

### Error responses

| Status | Description |
|---|---|
| `400` | Invalid `session_id`/`step_id` path parameters, the request body fails the step's schema, or an upload key in the body doesn't belong to this session |
| `401` | Invalid or missing API key |
| `403` | The session belongs to a different organization |
| `404` | Session or step not found, the step isn't part of this session's workflow, or no workflow execution state exists for this session |
| `409` | The session is terminal, the step is out of order or not in a submittable state, a concurrent submission conflict occurred, or a multi-phase `complete` action was called before `start` or before its result was ready |
| `429` | Too many requests |
| `501` | This step type isn't runnable through the API yet |

<ResponseExample>

```json 200 — ID_VERIFICATION completed
{
  "step_id": "ID_VERIFICATION",
  "step_status": "COMPLETED",
  "failure_reason": null,
  "current_step": 1,
  "attempts_remaining": 3,
  "session_status": "PENDING",
  "session_progress": "STARTED"
}
```

```json 200 — ID_VERIFICATION failed
{
  "step_id": "ID_VERIFICATION",
  "step_status": "PENDING",
  "failure_reason": "FACE_MISMATCH",
  "current_step": 0,
  "attempts_remaining": 2,
  "session_status": "PENDING",
  "session_progress": "STARTED"
}
```

```json 200 — FACE_LIVENESS start
{
  "step_id": "FACE_LIVENESS",
  "step_status": "IN_PROGRESS",
  "failure_reason": null,
  "current_step": 1,
  "attempts_remaining": 3,
  "session_status": "PENDING",
  "session_progress": "STARTED",
  "liveness": {
    "liveness_session_id": "12345678-90ab-cdef-1234-567890abcdef",
    "region": "us-east-1",
    "credentials": {
      "access_key_id": "ASIAEXAMPLE",
      "secret_access_key": "secretExampleValue",
      "session_token": "tokenExampleValue",
      "expiration": "2026-09-08T14:20:00.000Z"
    },
    "expires_at": "2026-09-08T14:20:00.000Z"
  }
}
```

```json 200 — FACE_LIVENESS complete (passed)
{
  "step_id": "FACE_LIVENESS",
  "step_status": "COMPLETED",
  "failure_reason": null,
  "current_step": null,
  "attempts_remaining": 3,
  "session_status": "VERIFIED",
  "session_progress": "COMPLETED",
  "liveness": {
    "status": "SUCCEEDED",
    "confidence": 93.1,
    "passed": true
  }
}
```

```json 409 — Out of order
{
  "error": "Workflow step is out of order",
  "current_step": 0,
  "step_id": "FACE_LIVENESS"
}
```

```json 409 — Liveness not started
{
  "error": "Face liveness has not been started for this session",
  "failure_reason": "FACE_LIVENESS_NOT_STARTED",
  "current_step": 1,
  "step_id": "FACE_LIVENESS"
}
```

</ResponseExample>
