---
title: Create Session Uploads
description: Get presigned URLs for files required by the current workflow step
api: POST /v1/sessions/{session_id}/uploads
---

```
POST /v1/sessions/{session_id}/uploads
```

Mints session-scoped presigned S3 upload URLs. Use this endpoint for ID/selfie captures and for config-driven files such as custom prompts, consent signatures, and document uploads.

Upload each file directly to its `upload_url`, then pass the returned `file_key` under the corresponding `slot` when you call [Submit Workflow Step](/api-reference/workflow-runner/submit-workflow-step).

<Info>
  For a config-driven upload, copy the exact slot from the current step's `requirements`. The endpoint validates it against that step and rejects slots belonging to another step or workflow configuration.
</Info>

## Request

### Headers

| Header | Required | Description |
|---|---|---|
| `x-api-key` | Yes | Your organization's API key |

### Path parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `session_id` | string (uuid) | Yes | The workflow session to upload files for |

### Body parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `files` | array | Yes | At least one file. No two entries may resolve to the same logical slot |
| `files[].file_name` | string | Yes | Original file name, used to derive the stored file extension |
| `files[].content_type` | string | Yes | MIME type of the file, e.g. `image/jpeg` |
| `files[].upload_type` | string | One of `upload_type` or `slot` | Legacy ID/selfie slot: `id_front`, `id_back`, `secondary_id_front`, `secondary_id_back`, `tertiary_id_front`, `tertiary_id_back`, or `selfie_front` |
| `files[].slot` | string | One of `upload_type` or `slot` | Exact config-driven slot returned by the current step's `requirements`. Maximum 255 characters |

Do not provide both `upload_type` and `slot` for the same file.

### Example: ID and selfie captures

<CodeGroup>

```bash cURL
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/uploads \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "files": [
      { "file_name": "id_front.jpg", "content_type": "image/jpeg", "upload_type": "id_front" },
      { "file_name": "selfie.jpg", "content_type": "image/jpeg", "upload_type": "selfie_front" }
    ]
  }'
```

```javascript Node.js
const response = await fetch(
  "https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/uploads",
  {
    method: "POST",
    headers: {
      "x-api-key": "YOUR_API_KEY",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      files: [
        { file_name: "id_front.jpg", content_type: "image/jpeg", upload_type: "id_front" },
        { file_name: "selfie.jpg", content_type: "image/jpeg", upload_type: "selfie_front" },
      ],
    }),
  }
);

const { signed_urls } = await response.json();
```

```python Python
import requests

response = requests.post(
    "https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/uploads",
    headers={"x-api-key": "YOUR_API_KEY"},
    json={
        "files": [
            {"file_name": "id_front.jpg", "content_type": "image/jpeg", "upload_type": "id_front"},
            {"file_name": "selfie.jpg", "content_type": "image/jpeg", "upload_type": "selfie_front"},
        ]
    },
)

signed_urls = response.json()["signed_urls"]
```

</CodeGroup>

### Example: config-driven document slot

Suppose the current `DOCUMENT_UPLOAD` step returns this requirement:

```json
{
  "documents": [
    {
      "id": "employment_letter",
      "description": "Upload a recent employment letter",
      "optional": false,
      "extractions": []
    }
  ]
}
```

Request a URL using that exact `id` as `slot`:

```bash
curl -X POST https://api.deepidv.com/v1/sessions/80e801af-991b-425c-96fc-f5e07d794253/uploads \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "files": [
      {
        "file_name": "employment-letter.pdf",
        "content_type": "application/pdf",
        "slot": "employment_letter"
      }
    ]
  }'
```

The same pattern applies to:

| Step | Where the slot comes from |
|---|---|
| `CUSTOM_PROMPT` | `requirements.upload_slots[]` or `requirements.prompts[].slot` |
| `DOCUMENT_UPLOAD` | `requirements.documents[].id` |
| `CONSENT`, `FACE_LIVENESS_CONSENT_SETTINGS` | `signature_upload` when `requirements.signature_required` is `true` |

### Upload the file

Send the raw bytes to the returned URL with the same `Content-Type` used when requesting it:

```bash
curl -X PUT "PRESIGNED_UPLOAD_URL" \
  -H "Content-Type: application/pdf" \
  --data-binary @employment-letter.pdf
```

Then submit the step using the returned `file_key`:

```json
{
  "uploads": {
    "employment_letter": "org-id/session-id/1717000000000-0-employment_letter.pdf"
  }
}
```

## Response

### 200 — Success

| Field | Type | Description |
|---|---|---|
| `signed_urls` | array | One entry per requested file |
| `signed_urls[].file_key` | string | Pass this value under `signed_urls[].slot` when submitting the current step |
| `signed_urls[].slot` | string | Logical step slot for this file. Always present |
| `signed_urls[].upload_type` | string | Echoes the requested legacy `upload_type`. Omitted for config-driven `slot` requests |
| `signed_urls[].upload_url` | string | Presigned URL — upload the raw file bytes to this URL with an HTTP `PUT` |

<Note>
  Upload URLs expire after a short time. Request them immediately before uploading, and submit the step promptly afterward.
</Note>

### Error responses

| Status | Description |
|---|---|
| `400` | Invalid request body, duplicate slot, or a dynamic slot not required by the current step. Invalid-slot responses include `invalid_slots` and `allowed_slots` |
| `401` | Invalid or missing API key |
| `404` | Session not found |
| `409` | Session is terminal or has no active workflow step for a dynamic upload |
| `429` | Too many requests |

<ResponseExample>

```json 200
{
  "signed_urls": [
    {
      "file_key": "3f9c1e2a-org/80e801af-991b-425c-96fc-f5e07d794253/1717000000000-id_front.jpg",
      "slot": "id_front",
      "upload_type": "id_front",
      "upload_url": "https://deepidv-sessions-uploads.s3.amazonaws.com/..."
    },
    {
      "file_key": "3f9c1e2a-org/80e801af-991b-425c-96fc-f5e07d794253/1717000000000-selfie_front.jpg",
      "slot": "selfie_front",
      "upload_type": "selfie_front",
      "upload_url": "https://deepidv-sessions-uploads.s3.amazonaws.com/..."
    }
  ]
}
```

```json 400 — Slot not valid for current step
{
  "error": "Upload slot is not required by the current workflow step",
  "invalid_slots": ["employment_letter"],
  "allowed_slots": ["custom-prompts-list_0"]
}
```

</ResponseExample>
